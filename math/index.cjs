const add = require("./add");
const div = require("./div");
const mul = require("./mul");
const sub = require("./sub");

// const { add, addTwoString } = data;
// console.log(add(1, 2));
// console.log(addTwoString("salom", "dunyo"));

module.exports = {
    add,
    div,
    mul,
    sub,
};
